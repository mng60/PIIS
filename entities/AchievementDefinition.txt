{
  "name": "AchievementDefinition",
  "type": "object",
  "properties": {
    "game_id": {
      "type": "string",
      "description": "ID del juego (vacio = logro global)"
    },
    "title": {
      "type": "string",
      "description": "Titulo del logro"
    },
    "description": {
      "type": "string",
      "description": "Descripcion del logro"
    },
    "icon_url": {
      "type": "string",
      "description": "URL del icono del logro"
    },
    "metric": {
      "type": "string",
      "enum": [
        "plays_count",
        "best_score",
        "wins_count",
        "single_run_score"
      ],
      "description": "Metrica que desbloquea el logro"
    },
    "threshold": {
      "type": "number",
      "description": "Valor umbral para desbloquear"
    },
    "win_score_min": {
      "type": "number",
      "description": "Puntuacion minima para contar como victoria (solo para metric=wins_count)"
    },
    "score_scale": {
      "type": "number",
      "default": 1,
      "description": "Factor de escala aplicado al score"
    },
    "score_offset": {
      "type": "number",
      "default": 0,
      "description": "Offset sumado al score escalado"
    },
    "is_active": {
      "type": "boolean",
      "default": true,
      "description": "Si el logro esta activo"
    },
    "sort_order": {
      "type": "number",
      "default": 0,
      "description": "Orden de visualizacion"
    }
  },
  "required": [
    "title",
    "metric",
    "threshold"
  ]
}